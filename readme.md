# profile

## Development

### Hot reload

Spin up development environment with live regen:

```shell
devenv up -d
```

Site is available at `http://localhost:4000`.

### Update dependencies

```shell
bundle update
bundix
```
