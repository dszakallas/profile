version: '3'
services:
  profile:
    build: .
#    user: "501:20"
    volumes:
      - ./src:/srv/jekyll
    ports:
      - 4000:4000
      - 35729:35729
    command: "jekyll serve --host 0.0.0.0 --port 4000 --future --incremental -l --livereload-port 35729"
